// This shows a simple build wrapper example, using the AnsiColor plugin.
node {
   stage('clone project'){
      checkout scm
   }
   stage('stage 1') {
      echo "abc"
   }
   stage('stage 2') {
      echo "def"
   }
}

